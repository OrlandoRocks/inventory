= form_for(divition_type, divition_type: 'form', html: {class: 'form-horizontal'}, validate: true) do |f|
  - if divition_type.errors.any?
    #error_explanation
      h2 = "#{pluralize(divition_type.errors.count, "error")} prohibited this divition_type from being saved:"
      ul
        - divition_type.errors.full_messages.each do |message|
          li = message
  .box-body
    .row.form-group
      = f.label 'Tipo de Division', class: 'col-sm-2 control-label'
      .col-sm-10
        = f.text_field :name, class: 'form-control', placeholder: 'Tipo de Division', autofocus: true
    .row.form-group
      = f.label 'Modelo', class: 'col-sm-2 control-label'
      .col-sm-10
        = f.text_field :model_part, class: 'form-control', placeholder: 'Modelo'

    .row.form-group
      = f.label 'Estatus', class: 'col-sm-2 control-label'
      .col-sm-10
        = f.check_box(:status, checked: true)

  .box-footer
    = f.button "<i class='fa fa-floppy-o'></i> #{t('helpers.submit.submit', model: 'Divisiones')}".html_safe,
            class: 'btn btn-primary btn-flat', type: 'submit'
    = link_to divition_types_path, class: 'btn btn-default btn-flat' do
      i.fa.fa-list
      =< t('.back')