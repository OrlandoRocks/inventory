section.content ng-controller="itemController"
  .row
    .col-xs-12 ng-init="init_orders(#{@users.to_json})"
      .box.box-primary
        .box-header.with-border
          h3.box-title
            | Nuevo pedido
        = render 'form_order'