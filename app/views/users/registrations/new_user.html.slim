section.content ng-controller="userController" ng-init=("accessless = #{@role_accessless}; roles=#{@roles.to_json}; companies=#{@companies.to_json}")
  .row
    .col-xs-12
      .box.box-primary
        .box-header.with-border
          h3.box-title
            = t('.title')
        = render 'form', user: @user
